# 2026-07-11 DataAnalysis PM 工作日志（续 2026-07-10 深夜）

- V1.7 机头/机尾跨类同图改造三阶段派工全部完成。

## 派工记录
- W1（w1-v17-services, coder subagent, 9m9s）：数据模型+服务层+单元测试。
  - DatasetItem 新增 category/pixel_factor/scaled 字段（向后兼容）。
  - data_processing.py 新增 scale_datasets_by_category 按类别批量 mm 缩放入口。
  - dataset_manager.py 新增 CATEGORY_LABELS/CATEGORY_PREFIXES/TIME_COLUMN、merge_by_category、merge_cross_category（outer join + [机头]/[机尾] 前缀 + NaN 独立）、merge_uncategorized。
  - 新增 tests/test_cross_category.py（8 用例），tests/test_scale_feature.py 补 5 个按类别用例（共 19）。
  - PM 验收时修 tests/test_descriptive_service.py 旧 E:\DEMO\DateAnalysis 硬编码为 Path 相对路径。

- W2（w2-v17-ui）：被截断/中断，只完成部分骨架，不计入正式交付。

- W2b（w2b-v17-ui, coder subagent）：在 W2 半成品上完成 UI 改造。
  - main_window.py：工具栏新增“导入机头文件/导入机尾文件”按钮（修复 W2 留下的?乱码）；factor 输入对话框（QInputDialog.getDouble）；QSettings 记忆 head/tail_pixel_factor；_on_category_import_done 给每个 DatasetItem 写 category/pixel_factor/scaled=False；补完 _merge_by_category / _merge_cross_category / _scale_category_datasets 三个槽；processing_panel.scale_category_requested 接到批量缩放。
  - dataset_panel.py：[机头]蓝(#2979FF)/[机尾]橙(#FF8F00) HTML 徽标；新增“机头合并/机尾合并/跨类合同图”按钮与对应 Signal。
  - processing_panel.py：“作用范围”下拉（当前数据集/所有机头/所有机尾）+ scale_category_requested Signal + 类别 factor 记忆同步。
  - column_panel/chart_config_panel/chart_panel 未改（跨类合并后列名唯一，多 Y 逻辑天然支持）。

- W3（w3-v17-docs-smoke, coder subagent, 22m46s 后上下文丢失）：留下半成品——PROJECT.md/README.md/docs/DEVELOPMENT.md 更新到 V1.7.0；app/__init__.py __version__='V1.7.0'；about 对话框 V1.7.0；tests/run_functional_tests.py 相对路径；tests/ui_smoke_test.py 改相对路径并补跨类路径但写入时出现字面 \n 语法错误。
  - PM 亲自收尾：
    1. 修 ui_smoke_test.py 字面 \n -> 真换行（UTF-8 LF）。
    2. 修 main_window.py QAction import 错误（新版 PySide6 从 QtGui 导入，不在 QtWidgets）。
    3. ui_smoke_test.py 加 pytest 入口 test_ui_smoke_offscreen()（自建 QApplication，不依赖 pytest-qt）。
    4. time_aggregation.py 天粒度 floor 从 pandas 弃用的 'd' 改为 'D'。
    5. 清理 __pycache__。

## 最终验收证据
- python -m compileall app tests -q -> exit 0，0 报错。
- python -m pytest tests/test_scale_feature.py tests/test_cross_category.py tests/test_descriptive_service.py tests/ui_smoke_test.py -> 37 passed (19+8+9+1)，54.82s。
- 唯一 warning：libpyside Failed to disconnect (None)（测试里首次 disconnect 前未连接，harmless，不阻塞交付）。

## 版本：V1.7.0 已就绪
- 代码位置：E:\DEMO\DataAnalysis\projects\dateanalysis-desktop
- 运行前需在该目录重建 venv 并 pip install -r requirements.txt。

## 遗留（V1.8 候选）
- 跨类导出（PNG/CSV/XLSX）专项实测。
- 文件夹批量导入机头/机尾（当前仅文件多选）。
- factor 修改后已缩放数据集的重算提示。
- PROJECT.md 底部历史段落重复（文档债）。
- 数据处理动作扩展（替换中位数/0/固定值、排序、筛选、去重）。
- 均值线按序列单独控制、大文件虚拟滚动、.xls 完整兼容。
- 时序监控 Phase 2（SPC/EWMA/Cpk/ACF）。
# 2026-07-11 DataAnalysis PM 工作日志（00:35 ~ 00:50）

- V1.8 P1 W4（w4-v18-p1, coder subagent, 6m25s）交付完成，PM 验收通过。

## W4 交付内容
- ① 文件夹批量类别导入：工具栏新增"导入机头文件夹/导入机尾文件夹"按钮；抽出 _import_category(mode, category) 统一入口；复用现有 _import_folder 的后台 Worker + QProgressDialog；0 回归未分类路径。
- ② factor 修改后重算提示：processing_panel 加 scale_warn_label（橙红字，默认隐藏）+ refresh_scale_hint_requested Signal；主窗口 _scale_category_datasets 在 factor 不一致时弹 QMessageBox.question 询问是否强制重缩放；选"是"→先通过 _revert_scaled_columns 把列名复原（去(mm)/（mm）4种后缀识别）并除回 old_factor（float32 误差级别）→ 再 scale_datasets_by_category(force_rescale=True)；选"否"→跳过已缩放并写日志；factor 相同时不打扰。
  - data_processing.py 新增 _strip_mm_suffix / _revert_scaled_columns，scale_datasets_by_category 新增 force_rescale/rescale_targets 参数。
- ③ 跨类导出冒烟：ui_smoke_test 追加跨类路径下的 PNG(趋势)/CSV+XLSX(描述统计)/CSV+XLSX(数据集) 导出断言；实测 5 个文件全部生成且非空（cross_trend.png 13.7KB，两个 csv/xlsx 均 >20B）；曲线数=2（两类各一条）；导出 API 对 [机头]/[机尾] 前缀 + (mm) 后缀的中文列名天然兼容，无需修。

## W4 PM 验收证据
- python -m compileall app tests -q → 0 报错
- pytest test_scale_feature / test_cross_category / test_descriptive_service → 36 passed
- pytest ui_smoke_test → 1 passed in 50.52s（新增第 374 行同样的 disconnect RuntimeWarning，test-only 无害）
- 改文件数 4，远小于上限 8；未碰 PM 元数据、未跑 GUI、QAction import 未被破坏

## W4 披露的小技术债
- float32 乘法的"复原"是 /old_factor 反向，存在 1e-7 量级舍入误差；要精确逆需要保留原始副本（超 W4 范围）。
- 工具栏现在 6 个按钮（导入文件/文件夹/机头文件/机尾文件/机头文件夹/机尾文件夹+导出+打开日志+清空+关于），默认 1400 宽够用，窄屏可能拥挤（后续可改成下拉）。
- W5（w5-v18-p2-cleanup, coder subagent, 28m04s）交付，PM 验收通过。
  - tests/ui_smoke_test.py：新增 _safe_disconnect_changed helper（warnings.catch_warnings + simplefilter('ignore', RuntimeWarning) + try/except 兜底），替换三处 disconnect 调用，RuntimeWarning 彻底消失。
  - PROJECT.md：文档债复核，确认章节标题不重复、V1.0~V1.7.0 功能点全覆盖；补入缺失的 ADR-014/015/016（float32 单精度语义 / 自动排除时间日期列 / ProcessingRule exclude_mode 向后兼容）；ADR-004/007 在代码库无引用，未虚构占位。
  - PM 验证：compileall 0 错；pytest tests/ -q -W error → 37 passed in 46.71s，0 warnings；PROJECT.md UTF-8 无 BOM、LF、无重复 ## 章节。

## V1.8 P1+P2 收尾后当前基线
- 37 passed / 0 warnings（-W error 强证）
- 代码位置：E:\DEMO\DataAnalysis\projects\dateanalysis-desktop （V1.7.0 基础 + W4 文件夹批量/factor重缩放/跨类导出 + W5 警告清理/文档债）

## 剩余 Backlog（未派工）
- P2：数据处理动作扩展（替换中位数/0/固定值、排序、筛选、去重）
- P2：均值线按序列单独控制、大文件虚拟滚动、`.xls` 完整兼容
- P3：更多图表类型、时序监控 Phase 2 (SPC/EWMA/Cpk/ACF)
- P3：GitHub 上传（等 Owner 提供仓库信息）
# 2026-07-11 DataAnalysis PM 工作日志（08:22 ~ 11:27，上午续）

## 08:22 尘醒首次消息"环境你帮我搞定吧"
- PM 亲自搭建 venv：E:\DEMO\DataAnalysis\projects\dateanalysis-desktop\.venv（Python 3.12.4）\n- 依赖安装：PySide6 6.11.1 / pandas 3.0.3 / numpy 2.5.1 / openpyxl 3.1.5 / pyqtgraph 0.14.0 / pytest 9.1.1 / pytest-qt 4.5.0 / xlrd 2.0.2
- 单测验证：36 个非UI用例通过
- 写两个启动 bat：启动 DateAnalysis.bat（pythonw 无黑框）、运行测试.bat
- ui_smoke_test 在 PySide6 6.11 offscreen 下卡死，标记为"需桌面会话手动验证"

## 09:00 cron 跟进
- 向项目群发送试用跟进消息，告知 venv 已建好、bat 可用

## 09:22 尘醒反馈两个痛点 → W6/W7 一波
1. **缩放排除列不够**：机尾"未脱模-s""指数-s"是数值列但不该乘 factor
2. **同图多曲线量级差大，趋势看不清**

### W6（w6-v18-p2-fixes, gpt-5.5 subagent）
- A. 按类别默认排除列
  - QSettings 存 head_exclude_columns（默认"时间"）/ tail_exclude_columns（默认"未脱模-s,指数-s"）
  - _ask_category_factor 升级为自定义 QDialog：QDoubleSpinBox(factor) + QLineEdit(排除列逗号分隔)，取消即整体取消导入
  - scale_datasets_by_category 用 exclude_mode="auto" + 默认排除列（auto 时间列 + 配置列同时生效）
- B. Y轴模式三选一下拉（ChartOptionsPanel "Y轴显示"组）
  - shared（共用Y轴，默认旧行为）
  - normalized（min-max归一化0-1，常数列保护为0，仅绘图副本不改数据）
  - dual（双Y轴：首条左轴，其余右 ViewBox，XLink 联动，图例合并）
- 双Y轴实现：chart_panel 新增 _vbrights / _right_curves / _right_mean_items，vb1.sigResized/sigRangeChanged 同步 vb2 geometry，鼠标 hover 区分轴归属
- 新增测试：test_w6_chart_options / test_w6_exclude_cols / test_w6_normalize / test_w6_dual_axis（共12个）
- PM 本地验收：42 passed in 2.09s

### 尘醒追加"双Y轴也做了"（09:25-09:41）
- 双Y轴实际已包含在 W6 交付里（Worker 一次做了三模式）
- 10:13 尘醒说"两个Y轴也还是不够用"——确认双Y轴只能2条量级，需要更通用方案
- 推荐方案1小多图(Small Multiples)，尘醒选方案1

### W7（w7-small-multiples, gpt-5.5 subagent）
- Y轴模式加第4项：small_multiples"小多图（每列独立子图，共享X轴）"
- chart_panel 新增 _sm_widget = pg.GraphicsLayoutWidget，n 行 1 列 PlotItem，setXLink 联动，底行显示 X 轴/日期轴
- 每个子图独立 Y 自动 range、独立 PlotDataItem、独立均值 InfiniteLine（省略 TextItem 避免拥挤）
- clear() 切换显隐：shared/normalized/dual 用 plot_widget，small_multiples 用 _sm_widget
- 修复导出 bug：新增 current_export_widget()，小多图下导出截 _sm_widget；has_plotted_data() 守卫两个控件
- 竖线光标跨子图联动（本轮实现），tooltip 跨子图未做（标记 P2）
- 新增测试 test_w7_small_multiples.py（7个，含导出目标切换回归）
- PM 本地验收：55 passed in 2.75s，0 warnings，compileall 0 报错

## 10:58 尘醒提出工艺窗口分析需求
> "当'中心点Y、中指距或虎口距'这一列或多列在多少范围内，'指数-s'列的值会稳定在 3、4、5"

### 数据探查（11:06-11:15）
- 路径：E:\项目\脱模优化\1-数据\20260711，共14个CSV（7个视觉D3_7#_B1_85_*.csv + 7个脱模长度_135_*.csv）
- **关键发现**：
  1. CSV 是 GBK 编码（列名默认UTF-8读会乱码）
  2. 两组文件列完全不同，只有"时间"是公共键
     - D3视觉组：时间/虎口距/拇指距/中指距/中心点x/中心点y ≈ 9.5万行，2.25Hz
     - 脱模长度组：时间/未脱模-s/指数-s/食指距-s/中指距-s/无名指距-s/小指距-s ≈ 10.6万行，2.5Hz
  3. 指数-s 分布：4占82.4%（87696）、5占16.9%（17965）、0占0.76%（813，启动异常段）、1=3条、3=1条
  4. 指数=3样本严重不足（仅1条），需要后续批次补充
- 遵循数据红线：不出workspace、不贴原始数据到聊天，只输出统计结论

### 11:15-11:25 方案对齐
- 尘醒确认：两系统同一工控机时间同步，按时间就近匹配（±1s），指数=3样本后续补
- 功能要**集成进软件**，AI分析也集成到软件里输出到界面
- AI 兼容 OpenAI/DeepSeek 双预设 + 自定义；先用公司给的 codex key（即当前 Gateway 使用的 OPENAI_API_KEY）

## V1.9 W8 方案设计（待派工）
- 新Tab「工艺分析」（右侧 tabs 加第4个）
- W8a 本地分析引擎（派工中）：
  1. 数据选择控件（数据集/状态列/目标值多选/特征列多选/时间列自动识别）
  2. 时间就近对齐（pandas.merge_asof, tolerance=1s, direction='nearest'）
  3. 单变量工艺窗口：μ/σ/p5/p25/p50/p75/p95/min/max，μ±σ 和 μ±2σ 两档
  4. 轻量贪心决策树（2-3层，基尼），可读 if-then 规则
  5. ANOVA F 值特征重要性
  6. 可视化：每特征列按状态分面的箱线图+小提琴（pg，小多图风格）
  7. 结果导出 CSV/PNG
- W8b AI解读面板（W8a 后再派）：
  - 预设：OpenAI（api.openai.com/v1，默认 gpt-4o-mini）/ DeepSeek（api.deepseek.com/v1，deepseek-chat）/ 自定义
  - API Key 优先读 os.environ["OPENAI_API_KEY"]（Gateway 已注入），否则 QSettings 持久化（不写日志/异常/导出）
  - 只传聚合统计（样本数/μ/σ/分位数/Top规则/特征重要性），不上传原始数据
  - 异步 QThread + 30s 超时 + 错误提示
  - 返回中文解读显示在只读文本框，支持重新生成

## 当前基线（截至 W7）
- compileall 0 报错
- pytest tests/test_scale_feature.py tests/test_cross_category.py tests/test_descriptive_service.py tests/test_w6_*.py tests/test_w7_*.py -q -W error → 55 passed in 2.75s，0 warnings
- venv 可用，启动 bat 可用

## Backlog 更新
- P1（W8 进行中）：工艺窗口分析 + AI 解读（V1.9 旗舰功能）
- P2：数据处理动作扩展、均值线按序列开关、xls兼容、大文件虚拟滚动、工具栏窄屏、float32 复原精度、小多图跨子图 tooltip、归一化+小多图组合
- P3：更多图表、SPC/EWMA/Cpk/ACF、GitHub上传

## 承诺账本
- C004（W6+W7）已达成交付（55 passed）
- C005（W8 工艺窗口分析+AI）已登记 PENDING，即将派 W8a
